# Credentials and local configuration
.env
.env.*
!.env.example

# Python and editor state
__pycache__/
*.py[cod]
.venv/
.idea/
.vscode/
.DS_Store

# Local dependency/runtime links
node_modules

# Downloaded source data. Keep only the directory documentation.
raw/*
!raw/README.md
*.part

# Normalized transcripts, calibration outputs, and historical classifications.
# The five canonical resume-state JSONLs are deliberate public exceptions.
classification/*
!classification/README.md
!classification/gemma4_26b_a4b_audit_v5_extension_levels.jsonl
!classification/gemma4_26b_a4b_audit_v5_extension_levels_errors.jsonl
!classification/manually_reviewed_clear_examples.jsonl
!classification/manually_reviewed_potential_examples.jsonl
!classification/manually_reviewed_none_examples.jsonl

# Generated tables, diagrams, workbooks, manuscripts, and QA renders.
results/*
!results/README.md
outputs/

# Ephemeral audit/review checkpoints and one-off record application scripts.
runtime/
resume_state/

# Local-only workbook builder depends on the bundled Codex artifact runtime and
# contains machine-specific paths; the portable CSV/JSON generator is tracked.
build_turn_distribution_workbook.mjs

*.log
*.docx
*.xlsx
